#!/usr/bin/env ruby

source 'https://supermarket.getchef.com'

metadata

group :integration do
  cookbook 'testrig', path: 'test/cookbooks/testrig'
  cookbook 'consul_wrapper', github: 'evilmartians/chef-consul-wrapper'
end
